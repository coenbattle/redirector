export default function Home() {
  return (
    <main style={{ padding: '2rem', fontFamily: 'sans-serif' }}>
      <h1>BulkInfo.link Redirector</h1>
      <p>Use this domain to redirect short paths like <code>/youtube</code> or <code>/apple</code> to any external URL.</p>
    </main>
  )
}
